import { StyleOverrideDirective } from './style-override.directive';

describe('StyleOverrideDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleOverrideDirective();
    expect(directive).toBeTruthy();
  });
});
