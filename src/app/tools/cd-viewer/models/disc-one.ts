import { createCaseConfig } from './template';

/**
 * Defines the first CD case in the collection
 * Represents the "Phantasia" album by An × Feryquitous
 * 【✓】
 */
export const discOne = createCaseConfig(
  1,
  "Phantasia",
  "An × Feryquitous"
); 