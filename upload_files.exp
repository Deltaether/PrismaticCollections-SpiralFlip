#!/usr/bin/expect -f

set timeout 300
set password "KC361kLC"

# Upload the deployment package
spawn scp -o StrictHostKeyChecking=no phantasia-website-latest.tar.gz root@212.227.85.148:/tmp/

expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    "100%" {
        puts "File upload completed successfully"
    }
    timeout {
        puts "Upload timeout"
        exit 1
    }
}

expect eof